// Конфигурация аналитики «Культура» — вариант с Google-Формой (без скриптов).
//
// Как заполнить — см. analytics/README.md. Нужно:
//   1) action  — адрес отправки формы (…/formResponse);
//   2) fields  — id полей формы (entry.XX…) в порядке: type, master, where, page, ts.
// Пока action пустой — трекинг выключен, сайт работает как обычно.
window.KULTURA_FORM = {
  action: "",
  fields: {
    type:   "entry.0000000000",
    master: "entry.0000000000",
    where:  "entry.0000000000",
    page:   "entry.0000000000",
    ts:     "entry.0000000000"
  }
};

// Необязательно: адрес опубликованного CSV таблицы ответов — только для страницы
// admin.html. Можно не задавать здесь, а ввести прямо в админке (сохранится в
// браузере). Оставьте "", если пользуетесь сводкой внутри Google-таблицы.
window.KULTURA_CSV_URL = "";
